{
  "name": "jiuku-company-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "npx gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.3.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^4.0.0",
    "@vitejs/plugin-vue": "^4.1.0"
  }
}